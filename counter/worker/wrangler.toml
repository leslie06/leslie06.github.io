name = "counter"
main = "worker.js"
compatibility_date = "2026-01-01"

# database_id 填 `wrangler d1 create counter` 打印出来的那一串
[[d1_databases]]
binding = "DB"
database_name = "counter"
database_id = "6ae98352-528f-4c97-8c22-ba0dc12c3c47"

# HASH_SALT 和 ADMIN_TOKEN 是密钥，不写在这个文件里（会进 git）。
# 用 wrangler secret put 设，见 README。
